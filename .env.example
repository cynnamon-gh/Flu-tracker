# Twilio credentials - get these from https://console.twilio.com
TWILIO_ACCOUNT_SID=your_account_sid_here
TWILIO_AUTH_TOKEN=your_auth_token_here
TWILIO_PHONE_NUMBER=+1XXXXXXXXXX

# Encryption key for PII (phone numbers at rest)
# Generate one with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your_fernet_key_here

# Database paths (defaults are fine for local dev)
IDENTITY_DB_PATH=data/identity.db
HEALTH_DB_PATH=data/health.db
